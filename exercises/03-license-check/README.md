Exercise 03 — License header detection and basic license-check script

Goal
- Build a small script to detect whether a source file contains a license header.
- Use tests to validate behavior and practice cautious acceptance of AI suggestions.

Instructions
1. Open `license_checker.py`.
2. Implement `has_license_header(path: str) -> bool` which returns True if a license header is found.
3. Run tests with `pytest -q`.
