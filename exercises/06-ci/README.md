Exercise 06 — CI & Automated tests (optional)

Goal
- Add or inspect a simple CI workflow that runs tests on push.
- Understand how automation helps verify code quality in collaborative projects.

Instructions
- Review `.github/workflows/python-tests.yml` to see the test workflow. This workflow will run `pytest` on pushes and PRs.
